<h1>Groups</h1>

<% @group.each do |group| %>
<p>
  <a href="<%= group_path group.id %>">
    <button type="button" class="btn btn-warning"><%= group.name %></button>
  </a>
</p>
<% end %>
<%= link_to "New group", new_group_path %>

